stage: teacher_tiny_training
project_root: /home/ubuntu22/tgpawb_world_model
seed: 42

data:
  token_shard_dir: /home/ubuntu22/tgpawb_world_model/data/token_shards/dummy_toy
  shard_glob: tokens_shard_*.pt
  split: toy

model:
  token_dim: 768
  hidden_dim: 512
  output_dim: 768
  num_layers: 2
  dropout: 0.0
  pool: mean

training:
  device: cuda_if_available
  batch_size: 4
  max_steps: 30
  learning_rate: 0.001
  weight_decay: 0.0001
  num_workers: 0
  log_every: 1
  checkpoint_every: 30
  max_grad_norm: 1.0

output:
  run_root: /home/ubuntu22/tgpawb_world_model/runs
  run_name: teacher_dummy_tiny_v1
  save_checkpoint: true
  save_metrics: true
